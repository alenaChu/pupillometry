# fill src_dir to process the whole directory OR video_path to process 1 video
src_dir:  ../data/eyes/mov
video_path: #../data/eyes/video_2_3.avi
prefix: 'track_'    # prefix to output video_path
output_dir: results/

frames_to_process: #[400, 446] # Frame range to process, like [0, 500]. Leave blank to process full video

apply_tracker: true
save_eye_patches: false
debug: false

